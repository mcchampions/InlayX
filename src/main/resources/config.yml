# InlayX 配置文件
# 宝石定义文件位于 plugins/InlayX/gems/ 目录下
# 所有宝石参数(属性, 掉落, 镶嵌)均在各自文件中独立配置

# 插件通用设置
settings:
  # 默认为 zh_cn
  # 可使用的语言为 mc 原版支持的语言列表, 语言代码也须与之对应(用于获取原版物品名)
  # 当前可用语言: zh_cn  zh_tw  en_us
  language: zh_cn
  gui_title: "宝石镶嵌"
  # 宝石槽位有关设置
  socket:
    header: "&7------- 宝石槽位 -------"
    footer: "&7======================"
    max_sockets: 8
    # 快捷镶嵌开关
    quick_socket:
      # 右键快捷镶嵌(主手持宝石, 副手持装备, 右键镶嵌)
      right_click: true
      # 背包拖拽快捷镶嵌(光标持宝石, 点击装备镶嵌)
      drag: true
    # 宝石槽位显示模板(仿宝石 display_pattern)
    # 各模板可用的变量分别标注在对应配置项上
    display_pattern:
      # 空槽模板, 可用变量: 
      #  {gemTypeColor} 宝石类型颜色  {gemTypeName} 宝石类型名称  {gemTypeId} 宝石类型ID
      # 模板必须保证不同宝石类型渲染出的行互不相同, 默认模板通过 {gemTypeColor}/{gemTypeName}/{gemTypeId} 提供差异;
      # 若多个类型渲染结果相同, 只会被识别为第一个类型
      empty: "{gemTypeColor}◇ {gemTypeName}宝石槽位"
      # 已镶嵌模板(列表, 每项为一行 lore), 可用变量:
      #  {gemTypeColor} 宝石类型颜色  {gemName} 宝石名称  {gemLevelStars} 宝石等级星星表示(★)  {gemLevel} 宝石等级数字表示(1)
      #  {gemTypeName} 宝石类型名称   {successRate} 镶嵌成功率(99%)  {destroyOnFailure} 失败是否破坏(是/否)  {gemId} 宝石id
      #  {gemDisplayName} 解析后的宝石显示名(gem display_pattern 渲染结果)
      # 专有变量: 
      #  {attributeLores} 宝石属性 lore(只在一行中单独存在该变量时解析)
      filled:
        - "{gemTypeColor}◆ {gemDisplayName}"
        - "{attributeLores}"
      # 属性行模板(filled 中 {attributeLores} 展开时逐行应用), 可用变量: 
      #  {gemTypeColor} 宝石类型颜色  {gemName} 宝石名称  {gemLevelStars} 宝石等级星星表示(★)  {gemLevel} 宝石等级数字表示(1)
      #  {gemTypeName} 宝石类型名称   {successRate} 镶嵌成功率(99%)  {destroyOnFailure} 失败是否破坏(是/否)  {gemId} 宝石id
      # 专有变量: 
      #  {attributeLore} 当行对应的属性 lore
      per_line_attribute_lore: "{gemTypeColor}  {attributeLore}"
  # 宝石设置
  gem:
    # 宝石 displayName 与 lore 显示模板
    # 可用变量: 
    #  {gemTypeColor} 宝石类型颜色  {gemName} 宝石名称  {gemLevelStars} 宝石等级星星表示(★)  {gemLevel} 宝石等级数字表示(1)
    #  {gemTypeName} 宝石类型名称   {successRate} 镶嵌成功率(99%)  {destroyOnFailure} 失败是否破坏(是/否)  {gemId} 宝石id
    display_pattern:
      display_name: "{gemTypeColor}{gemName} {gemLevelStars}"
      # 专有变量:
      #  {attributeLores} 属性 lore(只在一行中单独存在该变量时解析)
      #  {equipmentFilterLores} 设备过滤 lore(只在一行中单独存在该变量时解析)
      lore:
        - "&7类型: {gemTypeName} 宝石"
        - "&7等级: {gemLevel}"
        - "&7成功率: {successRate}"
        - "&7失败时损坏: {destroyOnFailure}"
        - ""
        - "{attributeLores}"
        - ""
        - "{equipmentFilterLores}"
        - ""
        - "&e右键点击装备可直接镶嵌"
        - "&e或使用 /gem socket 打开镶嵌界面"
      # 每一行属性 lore 的显示模板
      # {attributeLore} 当行对应的属性 lore, 在宝石配置文件中, 该变量仅对当前配置有效
      per_line_attribute_lore: "{gemTypeColor}{attributeLore}"
      # 装备过滤显示模板
      equipment_filter_lore:
        # 这里为 {equipmentFilterLores} 的具体显示方法
        # 支持变量: {equipmentDisplayLore} (只在一行中单独存在该变量时解析)替换为所有装备的列表, 每行的显示效果由 per_line_equipment_display_lore 配置决定
        pattern:
          none:
            - "&7允许镶嵌在所有装备"
          white_list:
            - "&7允许镶嵌在以下装备:"
            - "{equipmentDisplayLore}"
          black_list:
            - "&7不允许镶嵌在以下装备:"
            - "{equipmentDisplayLore}"
        # 每行装备显示模板
        # 专用变量:
        #  {equipmentName} 装备显示名(钻石剑)  {equipmentId} 装备ID(DIAMOND_SWORD)
        per_line_equipment_display_lore: "&7  - {equipmentName}"
    # 掉落系统
    drop:
      # 是否启用本插件自带的 掉落物系统
      enable: false
      # 多个掉落来源之间的关系: FIRST 首次掉落后停止, ALL 掉落后继续判断剩余来源
      mode: FIRST
    # 提取设置
    extract:
      # 宝石提取成功率 (0.0 - 1.0), 失败时宝石将碎裂消失
      success_rate: 1
    # 给予设置
    give:
      # 玩家背包已满时的处理方式: drop(掉落在玩家旁边) / deny(不予发放)
      full_inventory: drop
  # 宝石类型配置: 键为宝石类型 ID(宝石定义文件中的 type 字段), 
  # name 为显示名称, color 为槽位与提示颜色(支持 ChatColor 颜色名)
  gem_types:
    ATTACK:
      name: "攻击"
      # &c
      color: RED
    DEFENSE:
      name: "防御"
      # &9
      color: BLUE
    HEALTH:
      name: "生命"
      # &a
      color: GREEN
    SPEED:
      name: "速度"
      # &b
      color: AQUA
    LUCK:
      name: "幸运"
      # &6
      color: GOLD
    MAGIC:
      name: "魔法"
      # &d
      color: LIGHT_PURPLE
    ELEMENTAL:
      name: "元素"
      # &e
      color: YELLOW
    UTILITY:
      name: "实用"
      # &7
      color: GRAY
    SPECIAL:
      name: "特殊"
      # &5
      color: DARK_PURPLE
  # 播放声音设置
  sounds:
    # 镶嵌声音
    socket:
      success:
        enable: true
        sound: ENTITY_PLAYER_LEVELUP
        volume: 1.0
        pitch: 1.0
      failure:
        enable: true
        sound: BLOCK_ANVIL_BREAK
        volume: 1.0
        pitch: 1.0
    # 提取声音
    extract:
      success:
        enable: true
        sound: ENTITY_PLAYER_LEVELUP
        volume: 1.0
        pitch: 1.0
      failure:
        enable: true
        sound: BLOCK_ANVIL_BREAK
        volume: 1.0
        pitch: 1.0

config-version: 4
# 自定义宝石(不建议在此处配置除非只有几种宝石, 或许更方便)
# gems:
#   example_gem:
#     name: "示例宝石"
#     type: SPECIAL
#     level: 3
#     material: EMERALD
#     attributes:
#       - "物理伤害 +10"
#     socket:
#       success_rate: 0.80
#       destroy_on_failure: true
#     drop:
#       chance: 0.01
#       sources: [normal, mythic]
#       min_mob_level: 1
